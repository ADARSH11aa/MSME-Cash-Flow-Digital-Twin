# Runtime dependencies for the AI server (main.py) only.
#
# Deliberately NOT `uv export` — that emits 461 lines because pyproject.toml
# declares jupyter/jupyterlab/matplotlib/seaborn/xgboost for the training
# notebooks. None of those are imported by the serving path (verified against
# sys.modules after main.load_models()), and shipping them roughly triples
# both image size and build time.
#
# xgboost in particular is never imported: Model 1's artifact is a
# RandomForestRegressor despite what README.md says.
#
# Versions pinned to what the local .venv runs.

fastapi==0.141.1
uvicorn==0.52.3
starlette==1.6.0
pydantic==2.13.4
python-multipart==0.0.32
python-dotenv==1.2.3
requests==2.34.2

# Models 1/2/3/5
numpy==2.5.2
pandas==3.0.5
scikit-learn==1.9.0
scipy==1.18.0
joblib==1.5.3

# Model 5 (SHAP). Pulls numba + llvmlite + matplotlib transitively; that is
# ~73 MB of the server's resident memory.
shap==0.52.0
numba==0.67.0
llvmlite==0.49.0
cloudpickle==3.1.2
slicer==0.0.8
tqdm==4.70.0

# Model 4 (OCR). pytesseract is only a wrapper - the container also needs the
# `tesseract-ocr` system binary, installed in the Dockerfile.
pytesseract==0.3.13
pymupdf==1.28.2
pillow==12.3.0

# Model 6 (LLM narration). Optional at runtime: without GROQ_API_KEY the
# server still boots and only /narrate/invoice returns 503.
groq==1.6.0
